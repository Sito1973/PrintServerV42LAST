
# USAR IMAGEN SIN CONSTRUCCIÓN - TODO PRE-CONSTRUIDO
FROM node:20-slim
WORKDIR /app

# Copiar todo pre-construido (sin RUN commands)
COPY dist ./dist
COPY node_modules ./node_modules
COPY package*.json ./
COPY start.sh ./start.sh

ENV NODE_ENV=production
ENV PORT=3000
ENV HOST=0.0.0.0

EXPOSE 3000

CMD ["sh", "start.sh"]
